export enum env {
  DEV = 'development',
  PROD = 'production',
  TEST = 'test',
}

export enum dialects {
  MYSQL = 'mysql',
  PSQL = 'postgres',
  SQLITE = 'sqlite',
  MDB = 'mariadb',
  MSSQL = 'mssql',
}
